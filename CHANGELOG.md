# 更新日志

本项目的重要变更都会记录在此文件中。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.0] - 2026-09-23

首个正式发布版本。

### 新增

- 13 条学习赛道，覆盖零基础启蒙、Python、Java、数据结构、SQL、FastAPI、Spring Boot、Spring AI、Agent、多智能体、Linux 运维与 TypeScript 全栈。
- 35 节课程与 51 个检查点，每节课采用「生活比喻 → 核心直觉 → 代码实操 → 检查点自动判定」四段式教学。
- GitHub 陌生开源项目拆解实验室：架构蓝图、文件树、请求链路追踪、关键源码逐行注释、热修复挑战。
- 代码掌控力实训室：代码事故复盘、隐患机制剖析、加固方案对照。
- 代码审核与测试中心：可通性、可行性、安全性三维静态审计，以及全站自动化回归测试。
- 每日挑战系统：微算法题与代码事故选择题双题型，支持连续打卡。
- 成就与进度体系：经验值、连续打卡天数、徽章解锁，进度持久化于浏览器本地。
- 智能助教侧边抽屉：结合当前赛道、课程与编辑器代码上下文作答。
- 服务端代码执行沙箱：真实执行 Python 与 JavaScript，磁盘与 SQL 类操作采用确定性模拟回放。
- 本地智能引擎兜底：未配置云端密钥或网络不可达时，全部智能接口自动降级，功能不中断。

### 修复

- **修复 Windows 环境下代码沙箱完全不可用的问题**：原实现硬编码 `/tmp` 目录，在 Windows 上会解析为当前盘符根目录下不存在的 `\tmp\`，导致临时文件写入失败；现改为使用操作系统标准临时目录。
- **修复 Python 解释器探测失败的问题**：原实现硬编码 `python3` 命令，而 Windows 官方安装包提供的是 `python.exe` / `py.exe`；现按平台优先级自动探测并缓存可用命令。
- **修复报错信息净化的正则注入问题**：原实现用 `new RegExp(临时路径)` 替换路径，Windows 路径包含反斜杠会导致转义错误；现改为字符串替换并覆盖多种路径变体。
- 修复 Node 沙箱依赖 PATH 中 `node` 命令的问题，改用 `process.execPath` 保证可靠启动。
- 修复沙箱执行未设置工作目录的问题，避免用户代码读写项目目录文件。
- 修复沙箱进程继承服务端密钥环境变量的问题，用户代码不再能读取到凭据。
- 服务端口改为可通过 `PORT` 环境变量配置，避免端口冲突时无法启动。

### 变更

- 项目元数据规范化：`package.json` 更名并补充描述、许可证、作者字段。
- 环境变量示例文件重写为中文说明，明确各变量的必填性与降级行为。
- README 重写为完整项目手册，补充架构说明、接口清单、安全说明与二次开发指南。
- 新增 MIT 许可证、贡献指南、更新日志与编辑器统一配置。

### 移除

- 移除无关的平台元数据文件与模板残留。

## [0.0.0] - 2026-09-22

内部初始版本，完成平台主体功能搭建。
